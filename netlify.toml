[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Prerendered directories serve real pages; unknown routes use dist/404.html.
# Do not add a wildcard SPA rewrite, which would discard the route metadata.
