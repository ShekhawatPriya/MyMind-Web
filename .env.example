# Optional public deployment origin, without a trailing slash.
# Set this before building to generate canonical URLs and sitemap.xml.
# No credentials or native-app API keys belong in this website.
VITE_SITE_URL=
